# 🦷 Сайт стоматологической клиники

Красивый, современный персональный сайт для стоматолога, созданный на Vue 3 + Vite.
Вдохновлен дизайном профессиональных стоматологических клиник.

## ✨ Особенности

- **Современный дизайн** с градиентами и анимациями
- **Hero-секция** с призывом к действию (CTA)
- **Карточка врача** с подробной информацией
- **Блок "Почему выбирают нас"** с преимуществами
- **Секция услуг** с красочными карточками
- **Форма записи на прием** с валидацией
- **Карусель отзывов** пациентов
- **FAQ секция** с аккордеоном
- **Ссылки на мессенджеры** (WhatsApp, Telegram)
- **Адаптивная верстка** для всех устройств
- **Плавные анимации** и hover-эффекты

## 🚀 Быстрый старт

1. Установите зависимости (если еще не установлены):
```bash
npm install
```

2. Запустите сервер разработки:
```bash
npm run dev
```

3. Откройте браузер по адресу: http://localhost:5173/

## 📝 Настройка контента

### 1. Информация о враче

Отредактируйте файл `src/components/DoctorCard.vue`:

```javascript
const doctor = ref({
  name: 'Иванова Мария Петровна',  // ← Замените на реальное ФИО
  specialization: 'Врач-стоматолог',
  experience: '10+ лет опыта',
  education: 'Первый Московский Государственный Медицинский Университет им. И.М. Сеченова',
  specialties: [
    'Терапевтическая стоматология',
    'Эстетическая реставрация',
    // ... добавьте свои специализации
  ],
  description: 'Ваше описание...'
})
```

### 2. Преимущества клиники

Отредактируйте файл `src/components/FeaturesSection.vue`:

```javascript
const features = [
  {
    icon: '🏥',
    title: 'Весь спектр услуг',
    description: 'От гигиены до имплантации — лечим всё, что связано с улыбкой'
  },
  // ... добавьте свои преимущества
]
```

### 3. Услуги

Отредактируйте файл `src/components/ServicesSection.vue`:

```javascript
const services = [
  {
    title: 'Терапия',
    description: 'Лечение зубов и десен, кариеса, реставрация',
    icon: '🦷',
    color: '#667eea'
  },
  // ... добавьте свои услуги
]
```

### 4. Отзывы пациентов

Отредактируйте файл `src/components/ReviewsSection.vue`:

```javascript
const reviews = [
  {
    name: 'Анна Смирнова',
    text: 'Текст отзыва...',
    rating: 5,
    date: 'Октябрь 2024'
  },
  // ... добавьте реальные отзывы
]
```

### 5. FAQ (Частые вопросы)

Отредактируйте файл `src/components/FaqSection.vue`:

```javascript
const faqs = [
  {
    question: 'Больно ли лечить зубы?',
    answer: 'Ваш ответ...',
    isOpen: false
  },
  // ... добавьте свои вопросы
]
```

### 6. Контактные данные

Отредактируйте файл `src/components/ContactLinks.vue`:

```javascript
const contacts = ref({
  phone: '+79001234567',        // ← Замените на реальный номер
  whatsapp: '+79001234567',     // ← Номер для WhatsApp
  telegram: 'dentist_username'  // ← Username в Telegram
})
```

### 7. Часы работы

В файле `src/components/ContactLinks.vue` измените:

```html
<strong>Часы работы:</strong> Пн-Пт: 9:00 - 20:00, Сб: 10:00 - 18:00
```

## 🎨 Персонализация дизайна

... (прочий контент идентичен исходному)
