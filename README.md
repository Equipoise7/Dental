# Стоматологическая клиника (Vue 3 + Vite)

Тестовый прототип сайта стоматологии. Используются современные практики: Vue 3 Composition API, i18n, PWA, анимации через IntersectionObserver, маска телефона, интеграция Яндекс.Карт, SEO-разметка, GitHub Pages деплой.

## 🚀 Возможности
- Адаптивный интерфейс
- Страница с услугами, отзывами, FAQ
- Форма записи с валидацией и маской телефона `+7 (XXX) XXX-XX-XX`
- Асинхронная загрузка Яндекс.Карты (API ключ через `.env`)
- i18n (RU / EN переключатель в шапке)
- PWA манифест и автообновление SW
- SEO: meta, OpenGraph, Schema.org (Dentist)
- Анимации появления (IntersectionObserver)
- Unit-тест маски телефона (Vitest)

## 📁 Структура
```
src/
  components/      # UI компоненты
  composables/     # Переиспользуемая логика (phone, inView)
  i18n/            # Локализация
  App.vue          # Корневой компонент
  main.js          # Точка входа
server/             # Express API (локально)
```

## 🔑 Переменные окружения
Создайте файл `.env` на основе `.env.example`:
```
VITE_YANDEX_MAPS_API_KEY=ваш_ключ
```
Если ключ не задан – карта загрузится в тестовом режиме (может быть ограничена).

## 🧪 Запуск локально
```sh
npm install
npm run dev
```
Бэкенд (Express API) для обработки формы (локально):
```sh
cd server
npm install
npm run dev
```
Форма шлёт POST на `/api/appointment` (проксируется Vite -> порт 5174). На GitHub Pages backend недоступен: будет сетевой error и пользователь увидит сообщение.

## 🏗 Сборка
```sh
npm run build
npm run preview
```

## 🌐 Деплой на GitHub Pages
Автоматически: пуш в `main` запускает GitHub Actions workflow `.github/workflows/deploy.yml`.
Сайт будет доступен по адресу:
```
https://<ваш_аккаунт>.github.io/vue-tour-osetia/
```
(Базовый путь настроен через `vite.config.js` и переменную `GITHUB_PAGES`.)

## 📱 PWA
- Генерируется манифест
- Автообновление service worker
- Можно "Установить" на устройство (desktop/mobile)

## 🌍 i18n
Используется `vue-i18n`. Добавить новую локаль:
```js
// src/i18n/index.js
messages.en.hero.title = 'New title'
```

## 🛡 Ограничения тестового прототипа
- Нет реальной базы данных / CRM интеграции
- Нет защиты (rate limiting / captcha) на форме
- Backend не деплоится на GitHub Pages (только статика)
- Отзывы и услуги – моковые данные в массиве

## ✅ Тесты
Запуск unit тестов:
```sh
npx vitest run
```

## 🔧 Возможные дальнейшие улучшения
- Lazy-loading изображений (сейчас нет картинок)
- Интеграция реального API (email, Telegram, CRM)
- Улучшение Lighthouse (performance / a11y)
- Добавление e2e тестов (Playwright)
- CI для линтинга + тестов

## 📄 Лицензия
MIT (опционально — добавьте файл LICENSE при необходимости).

---
Если нужна помощь с деплоем или расширением функционала — напишите.

